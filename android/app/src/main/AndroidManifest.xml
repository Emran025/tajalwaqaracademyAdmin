<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- =================================================================== -->
    <!--                       PERMISSIONS SECTION                           -->
    <!-- All permissions must be direct children of the <manifest> tag.      -->
    <!-- =================================================================== -->

    <!-- Required to access the internet. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Required by the internet_connection_checker_plus package to check network state. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- You might need this permission if you are targeting Android 11 (API 30) or higher -->
    <!-- and are using packages that need to query other apps. -->

    <!-- (for workmanager) Allows the app to run tasks in the background and prevent the processor from sleeping. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- (for workmanager) Allows the app to be notified when the device boots up to re-schedule background tasks. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- For Android 13+ (API 33+), required to show notifications about background work.  -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>


    <!-- =================================================================== -->
    <!--                     APPLICATION DEFINITION                          -->
    <!-- =================================================================== -->
    <application
        android:label="tajalwaqaracademy"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true"> 

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

</manifest>